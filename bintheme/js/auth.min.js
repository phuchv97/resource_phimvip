$("#login-form").submit(function(t) {
    $("#login-submit").prop("disabled", !0), $("#login-loading").show();
    var e = $(this).serializeArray(),
        s = $(this).attr("action");
    $.ajax({
        url: s,
        type: "POST",
        data: e,
        dataType: "json",
        success: function(t) {
            0 == t.status && ($("#error-message").show(), $("#error-message").text(t.message), $("#login-submit").removeAttr("disabled"), $("#login-loading").hide()), 1 == t.status && window.location.reload()
        }
    }), t.preventDefault()
}), $("#dangky-form").submit(function(t) {
    $("#dangky-submit").prop("disabled", !0), $("#register-loading").show();
    var e = $(this).serializeArray(),
        s = $(this).attr("action");
    $.ajax({
        url: s,
        type: "POST",
        data: e,
        dataType: "json",
        success: function(t) {
            if ($(".error-block").hide(), 0 == t.status) {
                for (var e in t.messages) $("#error-" + e).show(), $("#error-" + e).text(t.messages[e]);
                $("#dangky-submit").removeAttr("disabled"), $("#register-loading").hide()
            }
            1 == t.status && window.location.replace('./')
        }
    }), t.preventDefault()
}), $("#forgot-form").submit(function(t) {
    $("#forgot-form button").text("Sending..."), $("#forgot-form button").prop("disabled", !0);
    var e = $(this).serializeArray();
    $.ajax({
        url: base_url + "user/forgotPassword",
        type: "POST",
        data: e,
        dataType: "json",
        success: function(t) {
            0 == t.status && ($("#forgot-error-message").show(), $("#forgot-error-message").text(t.message), $("#forgot-form button").text("Sent"), $("#forgot-form button").prop("disabled", !1)), 1 == t.status && ($("#forgot-success-message").show(), $("#forgot-success-message").text(t.message), $("#forgot-form button").text("Sent"), $("#forgot-form button").prop("disabled", !1))
        }
    }), t.preventDefault()
});